# Développement local du site (astro dev).
# Copie ce fichier en `frontend/.env` puis lance `npm run dev`.
#
# On pointe le site local vers le Strapi de PRODUCTION (sur le serveur),
# pour récupérer le vrai contenu sans relancer Strapi en local.
STRAPI_INTERNAL_URL=http://178.105.238.252:1337
PUBLIC_STRAPI_URL=http://178.105.238.252:1337

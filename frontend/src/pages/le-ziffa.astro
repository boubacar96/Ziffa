---
import Base from '../layouts/Base.astro';
// TODO (migration) : porter le contenu de la page statique de référence
//   ../../../le-ziffa.html
// en réutilisant les composants/classes existants, et brancher les parties
// dynamiques sur Strapi (cf. src/lib/strapi.js et l'exemple dans index.astro).
---
<Base title="Le ZIFFA — Histoire, valeurs & équipe | ZIFFA" description="Découvrir le festival — ZIFFA.">
  <header class="page-hero">
    <div class="page-hero-inner">
      <div class="breadcrumb"><a href="/">Accueil</a> &nbsp;/&nbsp; Le ZIFFA</div>
      <div class="eyebrow"><span class="arrow">→</span> Découvrir le festival</div>
      <h1>Le <span class="yellow">ZIFFA</span>.</h1>
      <p>Page en cours de migration vers Astro. Le contenu de référence existe
         déjà dans <code>le-ziffa.html</code> (site statique) et sera porté ici.</p>
    </div>
  </header>

  <section class="section">
    <p class="lead">Section à porter : reprends le balisage de <code>le-ziffa.html</code>
       et remplace les contenus fixes par des appels Strapi quand c'est pertinent
       (films, programme, jury, prix, partenaires, articles…).</p>
  </section>
</Base>

'use strict';

module.exports = {
  /**
   * Exécuté avant l'initialisation de l'application.
   */
  register(/* { strapi } */) {},

  /**
   * Exécuté avant le démarrage (seed, permissions, etc.).
   */
  bootstrap(/* { strapi } */) {},
};
